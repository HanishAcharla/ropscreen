# URL of the deployed FastAPI inference backend (see /server).
# Local dev (running `uvicorn app.main:app --port 7860` in /server):
NEXT_PUBLIC_API_URL=http://localhost:7860

# Production: your Cloud Run service URL, e.g.
# NEXT_PUBLIC_API_URL=https://ropscreen-api-xxxxx-uc.a.run.app
